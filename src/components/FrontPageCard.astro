---
const { cardIcon, cardTitle, cardDescription, targetLocation } = Astro.props;
import logo from "../images/logo.webp";
---

<a href={targetLocation}>
  <div
    class="flex flex-col shadow-card py-6 px-10 justify-center items-center rounded-xl h-full"
  >
    <!-- Temporary src logo until we import all logos needed -->
    <img src={logo.src} width={50} height={50} />
    <p class="font-bold mt-2">{cardTitle}</p>
    <p class="text-small px-8 text-center">{cardDescription}</p>
  </div>
</a>
